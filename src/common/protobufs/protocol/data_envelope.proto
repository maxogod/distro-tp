syntax = "proto3";

package protocol;

option go_package = "./protocol";

message DataEnvelope {
  string client_id = 1;
  int32 task_type = 2;
  bool is_done = 3;
  bool  is_ref = 4;
  bytes payload = 5;
}
